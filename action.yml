name: 'Auto Release Draft'
description: 'Drafts a GitHub release with the changes introduced by a newly created version tag'
author: 'Michael Mrnak'
outputs:
  release-url:
    description: 'The URL of the release that was drafted'
runs:
  using: 'node12'
  main: 'dist/index.js'
